train:
  optimizer: RMSprop
  loss: CrossEntropyLoss
  batch_size: 64
  epochs: 100
  saving_every_epochs: 10
  teacher_forcing_ratio: 0.5
test:
  input_file: './res/test_input.txt'
  result_file: './res/test_result.txt'
  validation_split: 0.2

max_checkpoints: 10
checkpoint_dir: './res/weight/'